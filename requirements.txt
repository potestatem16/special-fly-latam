# Changed requirements.txt to use more recent versions that are compatible with Python 3.13

# upgrade pip, setuptools, and wheel to the latest versions
# pip install --upgrade pip setuptools wheel
fastapi>=0.100.0
pydantic>=2.0.0
uvicorn>=0.20.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=3.0.5
jupyter # helps translate the notebook to python code